module Game.IO (
  loop,
  printC,
  printD,
) where

import Game.Core (Area, Cells, Difference)

loop :: Area -> Cells -> IO ()
loop = undefined

printC :: Area -> Cells -> IO ()
printC = undefined

printD :: Area -> Difference -> IO ()
printD = undefined
